# /db/EPMT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-epmt
endpoint: /db/EPMT
name: Plastic Material

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 04_DB_Properties.md
    section: "10. `/db/EPMT`"
    url: https://support.midasuser.com/hc/en-us/articles/35808376517913
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    variant: per_id
    path: /db/EPMT/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      Plastic Material Name
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: MODEL_TYPE
    description: >-
      Model Type · "TR"(Tresca) / "VM"(Von-Mises) / "MC"(Mohr-Coulomb) /
      "DP"(Drucker-Prager) / "MA"(Masonry) / "DM"(Concrete Damage)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

sdkRules:
  - id: db-epmt-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-epmt-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 04_DB_Properties.md line 905
  table: Specifications
  missingColumns:
    - heading: "Masonry 파라미터 (`\"MASONRY\"` object)"
      line: 936
      columns: [Default]
    - heading: "Masonry 파라미터 (`\"MASONRY\"` object)"
      line: 945
      columns: [Default]
    - heading: "Concrete Damage 파라미터 (`\"CONCDMG\"` object)"
      line: 956
      columns: [Default]
  # Additional parameter tables in this section were NOT merged. They are
  # usually conditional variants selected by a type/code field. Decide
  # whether they belong in this contract's fields, as nested `properties`,
  # or as a separate contract - do not assume the first table is the whole
  # schema.
  unmergedTables:
    - heading: "Tresca / Von-Mises 공통 파라미터 (`\"TRESCA\"` or `\"VMISES\"` object)"
      resolution: "The tables select among named payload objects; the manual does not state a scalar wire discriminator for their object-presence branches; left unmerged."
      fields: 5
      line: 912
    - heading: "Mohr-Coulomb / Drucker-Prager 공통 파라미터 (`\"MOHRCL\"` or `\"DRUCKER\"` object)"
      resolution: "The tables select among named payload objects; the manual does not state a scalar wire discriminator for their object-presence branches; left unmerged."
      fields: 6
      line: 922
    - heading: "Masonry 파라미터 (`\"MASONRY\"` object)"
      resolution: "The tables select among named payload objects; the manual does not state a scalar wire discriminator for their object-presence branches; left unmerged."
      fields: 5
      line: 936
    - heading: "Masonry 파라미터 (`\"MASONRY\"` object)"
      resolution: "The tables select among named payload objects; the manual does not state a scalar wire discriminator for their object-presence branches; left unmerged."
      fields: 6
      line: 945
    - heading: "Concrete Damage 파라미터 (`\"CONCDMG\"` object)"
      resolution: "The tables select among named payload objects; the manual does not state a scalar wire discriminator for their object-presence branches; left unmerged."
      fields: 7
      line: 956
