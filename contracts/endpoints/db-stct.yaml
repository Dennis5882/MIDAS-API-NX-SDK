# /db/STCT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-stct
endpoint: /db/STCT
name: Construction Stage Analysis Control Data
surface:
  className: ConstructionStageAnalysisControlData
  exportName: constructionStageAnalysisControlData
  modulePath: [db, analysisControl]
  payloadTypeName: ConstructionStageAnalysisControlDataPayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 12_DB_Analysis_Control.md
    section: 17. /db/STCT — Construction Stage Analysis Control Data
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-17
    - product: gen
      ref: db-read-sweep-2026-08-17

operations:
  - method: DELETE
    variant: per_id
    path: /db/STCT/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: bLAST_FINAL
    description: >-
      Final Stage Option (Last: true / Other: false)
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: FINAL_STAGE
    description: >-
      Construction Stage Name (when bLAST_FINAL false)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: iINC_NLA
    description: >-
      Analysis Type (Linear: 0 / Nonlinear : 1 / Material Nonlinear : 2)
    type: integer
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: iNLA_TYPE
    description: >-
      Stage Option (Independent: 0 / Accumulative: 1)
    type: integer
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

sdkRules:
  - id: db-stct-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-stct-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 12_DB_Analysis_Control.md line 2035
  table: Parameters — 공통 / 핵심
  # Additional parameter tables in this section were NOT merged. They are
  # usually conditional variants selected by a type/code field. Decide
  # whether they belong in this contract's fields, as nested `properties`,
  # or as a separate contract - do not assume the first table is the whole
  # schema.
  unmergedTables:
    - heading: Parameters — Erection Load (C.S. 출력용 사하중 구분)
      resolution: "The supplementary tables describe analysis options and partial conditions, but no complete discriminator condition applies to each field set; left unmerged."
      fields: 3
      line: 2046
    - heading: Parameters — Cable-Pretension / Initial Force Control
      resolution: "The supplementary tables describe analysis options and partial conditions, but no complete discriminator condition applies to each field set; left unmerged."
      fields: 7
      line: 2066
    - heading: Parameters — Initial Displacement / Camber / 기타
      resolution: "The supplementary tables describe analysis options and partial conditions, but no complete discriminator condition applies to each field set; left unmerged."
      fields: 12
      line: 2078
    - heading: "Parameters — Linear & Independent Stage"
      resolution: "The supplementary tables describe analysis options and partial conditions, but no complete discriminator condition applies to each field set; left unmerged."
      fields: 3
      line: 2101
    - heading: "Parameters — Nonlinear Analysis (`iINC_NLA` = 1 또는 2)"
      resolution: "The supplementary tables describe analysis options and partial conditions, but no complete discriminator condition applies to each field set; left unmerged."
      fields: 9
      line: 2109
    - heading: "Parameters — Time Dependent Effect (누가 단계, `iNLA_TYPE` = 1)"
      resolution: "The supplementary tables describe analysis options and partial conditions, but no complete discriminator condition applies to each field set; left unmerged."
      fields: 15
      line: 2123
