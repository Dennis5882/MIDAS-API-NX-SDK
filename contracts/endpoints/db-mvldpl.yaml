# /db/MVLDpl
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-mvldpl
endpoint: /db/MVLDpl
name: Moving Load Cases – Poland
surface:
  className: MovingLoadCasePoland
  exportName: movingLoadCasePoland
  modulePath: [db, movingLoads]
  payloadTypeName: MovingLoadCasePolandPayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 08_DB_Moving_Loads.md
    section: 17. /db/MVLDpl – Moving Load Cases – Poland
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    variant: per_id
    path: /db/MVLDpl/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: LCNAME
    description: >-
      Load Case Name
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DESC
    description: >-
      Description
    type: string
    requirement: optional
    documentedDefault: ""
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bAUTO_OPTIMIZE
    description: >-
      Moving Load Optimization
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bPERMIT_LOAD
    description: >-
      Load Case for Permit Vehicle
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: LOAD_MODEL
    description: >-
      Load Model (1=Vehicle S/2S/Permit, 2=Vehicle K, 3=Military)
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DEFAULT
    description: >-
      Sub-Load Cases
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

variants:
  - when:
      - path: bAUTO_OPTIMIZE
        equals: true
    source:
      table: Moving Load Optimization(bAUTO_OPTIMIZE=true)
      line: 2187
    fields:
      - key: AUTO_OPTIMIZE
        description: >-
          Sub-Load Cases
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
  - when:
      - path: bPERMIT_LOAD
        equals: true
    source:
      table: Load Case for Permit Vehicle(bPERMIT_LOAD=true)
      line: 2204
    fields:
      - key: PERMIT_LOAD
        description: >-
          Permit Vehicle
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: VEHICLE_LOAD_NAME
            description: >-
              Vehicle Name
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: REF_LANE
            description: >-
              Reference Lane
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: ECC
            description: >-
              Eccentricity
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: SCALE_FACTOR
            description: >-
              Scale Factor
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual

sdkRules:
  - id: db-mvldpl-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-mvldpl-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 08_DB_Moving_Loads.md line 2166
  table: Parameters
  # Additional parameter tables in this section were NOT merged. They are
  # usually conditional variants selected by a type/code field. Decide
  # whether they belong in this contract's fields, as nested `properties`,
  # or as a separate contract - do not assume the first table is the whole
  # schema.
  unmergedTables:
    - heading: Vehicle S/2S/Permit(LOAD_MODEL=1)
      resolution: "The LOAD_MODEL tables are nested under separate General Load and Moving Load Optimization groups. Their complete gates also depend on the enclosing bAUTO_OPTIMIZE/bPERMIT_LOAD state, which these table headings do not repeat; left unmerged so repeated LOAD_MODEL values cannot be published as overlapping variants."
      fields: 2
      line: 2175
      fieldNames: ["DEFAULT", "COMB_OPTION", "SUB_LOAD_DATAS", "VEHICLE_NAME",
                   "SCALE_FACTOR", "MIN_LOADED_LANE", "MAX_LOADED_LANE", "LANE_NAMES"]
    - heading: Vehicle K/Military(LOAD_MODEL=2/3)
      resolution: "The LOAD_MODEL tables are nested under separate General Load and Moving Load Optimization groups. Their complete gates also depend on the enclosing bAUTO_OPTIMIZE/bPERMIT_LOAD state, which these table headings do not repeat; left unmerged so repeated LOAD_MODEL values cannot be published as overlapping variants."
      fields: 2
      line: 2183
      fieldNames: ["DEFAULT", "VEHICLE_LOAD_NAME", "SUB_LOAD_DATAS", "LANE_NAMES"]
    - heading: Vehicle S/2S(LOAD_MODEL=1)
      resolution: "The LOAD_MODEL tables are nested under separate General Load and Moving Load Optimization groups. Their complete gates also depend on the enclosing bAUTO_OPTIMIZE/bPERMIT_LOAD state, which these table headings do not repeat; left unmerged so repeated LOAD_MODEL values cannot be published as overlapping variants."
      fields: 2
      line: 2189
      fieldNames: ["AUTO_OPTIMIZE", "MIN_VEHL_DIST", "LANE_NAME", "MIN_NUM_VEHICLE",
                   "MAX_NUM_VEHICLE", "COMB_OPTION", "OPTIMIZE_ITEMS", "VEHICLE_TYPE",
                   "VEHICLE_NAME", "SCALE_FACTOR"]
    - heading: Vehicle K/Military(LOAD_MODEL=2/3)
      resolution: "The LOAD_MODEL tables are nested under separate General Load and Moving Load Optimization groups. Their complete gates also depend on the enclosing bAUTO_OPTIMIZE/bPERMIT_LOAD state, which these table headings do not repeat; left unmerged so repeated LOAD_MODEL values cannot be published as overlapping variants."
      fields: 1
      line: 2199
      fieldNames: ["AUTO_OPTIMIZE", "VEHICLE_LOAD_NAME", "MIN_VEHL_DIST", "LANE_NAME",
                   "NUM_LOADED_LANES"]
