# /DESIGN/SRC/AIK-SRC2K/BC-ANAL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-src-aik-src2k-bc-anal
endpoint: /DESIGN/SRC/AIK-SRC2K/BC-ANAL
name: SRC 보 검토 수행

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 27_Design_SRC_AIKSRC2K.md
    section: "15. `DESIGN/SRC/AIK-SRC2K/BC-ANAL` — SRC 보 검토 수행"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: PERFORM_TYPE
    description: >-
      설계 계산 대상 타입 선택. ELEMS: 요소번호별, SECTIONS: 단면번호별, ALL: 전체 요소. — ALL=전체 요소;
      ELEMS=요소번호별; SECTIONS=단면번호별
    type: string
    requirement: optional
    documentedDefault: ALL
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 번호 입력.
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          개별 ID 지정
        type: array
        items:
          type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
      - key: TO
        description: >-
          ID 범위 지정 (예: '1to160')
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹 이름 지정
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
  - key: SECTIONS
    description: >-
      단면 번호 입력.
    type: array
    items:
      type: integer
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 27_Design_SRC_AIKSRC2K.md line 2442
  table: 파라미터
