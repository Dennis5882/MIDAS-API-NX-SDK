# /db/NLCT-M1
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-nlct-m1
endpoint: /db/NLCT-M1
name: Nonlinear Analysis Control (Hyper-S)
surface:
  className: NonlinearAnalysisControlHyperS
  exportName: nonlinearAnalysisControlHyperS
  modulePath: [db, analysisControl]
  payloadTypeName: NonlinearAnalysisControlHyperSPayload

products: [civil]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 12_DB_Analysis_Control.md
    section: 16. /db/NLCT-M1 — Nonlinear Analysis Control (Hyper-S)
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_civil
  records:
    - product: civil
      ref: db-write-sweep-2026-08-25

operations:
  - method: DELETE
    variant: per_id
    path: /db/NLCT-M1/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: LC_SCOPE
    description: >-
      Load Case Scope (전체: "ALL" / 선택: "SELECT", 기본 "ALL")
    type: string
    requirement: optional
    documentedDefault: ALL
    documentedOptional: true
    enum: [ALL, SELECT]
    safeToOmit: unverified
    provenance: manual
  - key: LOAD_CASE
    description: >-
      Load Case Name (LC_SCOPE="SELECT"로 신규 생성 시 필수. 기존 항목 수정 시 미지정하면 기존 값 유지)
    type: string
    requirement: conditional
    condition: 조건부
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's NonlinearAnalysisControlHyperS case completed
      a live create-read-update-delete round trip on civil without this field in
      its payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: NONLINEAR_TYPE
    description: >-
      Nonlinear Type ("GEOM" / "MATL" / "GEOM_MATL")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [GEOM, MATL, GEOM_MATL]
    safeToOmit: unverified
    provenance: manual
  - key: ITER_METHOD
    description: >-
      Iteration Method (Force: "FORCE" / Arc Length: "ARC" / Displacement:
      "DISP")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [FORCE, ARC, DISP]
    safeToOmit: unverified
    provenance: manual
  - key: LOAD_STEPS
    description: >-
      Load Steps 설정
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: CONV_CRITERIA
    description: >-
      Convergence Criteria
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: DISP
        description: >-
          변위 수렴 기준
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: stated elsewhere in the same section - the sentence at
        # line 1880 names the three sub-objects ("`DISP`(변위) / `LOAD`(하중)
        # / `WORK`(일) 각각 — 최소 1개는 `OPT_USE=true`") and the table below it
        # gives the two members each one holds. The parser reads tables,
        # so it took the members and lost the three objects they belong
        # to, and this contract carried CONV_CRITERIA as an empty object
        # until 2026-09-04. GET /info/db/NLCT-M1 declares DISP, LOAD and
        # WORK with exactly OPT_USE and VALUE. The "최소 1개" rule is a
        # cross-field constraint no per-field requiredness can express, so
        # each object stays optional and the sentence is kept verbatim
        # here.
        properties:
          - key: OPT_USE
            description: >-
              해당 기준 사용 여부
            type: boolean
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: VALUE
            description: >-
              허용오차 (0 초과 1 이하)
            type: number
            requirement: conditional
            condition: '`OPT_USE=true`일 때 필수'
            appliesWhen:
              - path: CONV_CRITERIA.DISP.OPT_USE
                equals: true
            documentedDefault: 0.001
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: LOAD
        description: >-
          하중 수렴 기준
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: OPT_USE
            description: >-
              해당 기준 사용 여부
            type: boolean
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: VALUE
            description: >-
              허용오차 (0 초과 1 이하)
            type: number
            requirement: conditional
            condition: '`OPT_USE=true`일 때 필수'
            appliesWhen:
              - path: CONV_CRITERIA.LOAD.OPT_USE
                equals: true
            documentedDefault: 0.001
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: WORK
        description: >-
          일 수렴 기준
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: OPT_USE
            description: >-
              해당 기준 사용 여부
            type: boolean
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: VALUE
            description: >-
              허용오차 (0 초과 1 이하)
            type: number
            requirement: conditional
            condition: '`OPT_USE=true`일 때 필수'
            appliesWhen:
              - path: CONV_CRITERIA.WORK.OPT_USE
                equals: true
            documentedDefault: 0.001
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
  - key: ADVANCED
    description: >-
      고급 설정 (미지정 시 서버 기본값 적용)
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's NonlinearAnalysisControlHyperS case completed
      a live create-read-update-delete round trip on civil without this field in
      its payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual

sdkRules:
  - id: db-nlct-m1-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-nlct-m1-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 12_DB_Analysis_Control.md line 1847
  table: Parameters — 공통
  # Additional parameter tables in this section were NOT merged. They are
  # usually conditional variants selected by a type/code field. Decide
  # whether they belong in this contract's fields, as nested `properties`,
  # or as a separate contract - do not assume the first table is the whole
  # schema.
  unmergedTables:
    - heading: "Parameters — LOAD_STEPS 객체 (`ITER_METHOD`별 사용 필드가 다름)"
      resolution: "The nested LOAD_STEPS, CONV_CRITERIA, and ADVANCED tables describe objects and conditions but do not state a complete wire discriminator for each table; left unmerged."
      fields: 11
      line: 1864
      fieldNames: ["STEP_MODE", "NUMBER_STEPS", "OUTPUT", "MANUAL_STEPS", "MIN_ARC_RATIO",
                   "MAX_ARC_RATIO", "MAX_ARC_INCREMENTS", "MASTER_NODE", "MAX_DISP",
                   "DIRECTION", "REF_NODE"]
    - heading: Parameters — ADVANCED 객체 (고급 비선형 설정, 전체 Optional — 미지정 시 서버 기본값 사용)
      resolution: "The nested LOAD_STEPS, CONV_CRITERIA, and ADVANCED tables describe objects and conditions but do not state a complete wire discriminator for each table; left unmerged."
      fields: 12
      line: 1889
      fieldNames: ["OPT_USE_DEFAULT", "STIFF_UPDATE_SCHEME", "ITER_BEFORE_STIFF_UPDATE",
                   "OPT_TERMINATE_ON_FAILED_CONV", "MAX_ITER_PER_INCREMENT",
                   "MAX_BISECTION_LEVEL", "OPT_SMART_BISECTION", "DIVERGENCE_THRESHOLD",
                   "OPT_ENABLE_LINE_SEARCH", "LINE_SEARCH_OPTION",
                   "MAX_LINE_SEARCH_PER_ITER", "LINE_SEARCH_TOL"]
  structuralTables:
    - heading: Parameters — CONV_CRITERIA 객체
      line: 1882
      paths: [CONV_CRITERIA.DISP, CONV_CRITERIA.DISP.OPT_USE,
              CONV_CRITERIA.DISP.VALUE, CONV_CRITERIA.LOAD,
              CONV_CRITERIA.LOAD.OPT_USE, CONV_CRITERIA.LOAD.VALUE,
              CONV_CRITERIA.WORK, CONV_CRITERIA.WORK.OPT_USE,
              CONV_CRITERIA.WORK.VALUE]
      note: >-
        Recorded as unmerged until 2026-09-04 on the reading that it stated no
        wire discriminator. It states no branch because there is none: the
        sentence above it says the same two members belong to each of three
        sub-objects, all three independently optional. Merged into the three
        objects the sentence names.
  prose:
    - line: 1880
      quote: >-
        `DISP`(변위) / `LOAD`(하중) / `WORK`(일) 각각 — 최소 1개는 `OPT_USE=true`:
      paths: [CONV_CRITERIA.DISP, CONV_CRITERIA.LOAD, CONV_CRITERIA.WORK]
