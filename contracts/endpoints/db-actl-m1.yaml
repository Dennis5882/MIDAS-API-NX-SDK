# /db/ACTL-M1
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-actl-m1
endpoint: /db/ACTL-M1
name: Main Control Data (Hyper-S)
surface:
  className: MainControlDataHyperS
  exportName: mainControlDataHyperS
  modulePath: [db, analysisControl]
  payloadTypeName: MainControlDataHyperSPayload

products: [civil]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 12_DB_Analysis_Control.md
    section: 2. /db/ACTL-M1 — Main Control Data (Hyper-S)
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_civil
  records:
    - product: civil
      ref: db-write-sweep-2026-08-25

operations:
  - method: DELETE
    variant: per_id
    path: /db/ACTL-M1/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: ARCD
    description: >-
      Auto Rotational DOF Constraint
    type: boolean
    requirement: optional
    documentedDefault: true
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ANRC
    description: >-
      Auto Normal Rotation Constraint
    type: boolean
    requirement: optional
    documentedDefault: true
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: CSECF
    description: >-
      Consider Section Stiffness Scale Factor
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: CRBAR
    description: >-
      Consider Reinforcement for Section Stiffness
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: TRS
    description: >-
      Transfer Reactions to Master Node
    type: boolean
    requirement: optional
    documentedDefault: true
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: CLATS
    description: >-
      Change Local Axis of Tapered Section
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: BMSTRESS
    description: >-
      Calculate Equivalent Beam Stresses
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: CLFORM
    description: >-
      Classical Formula for Solid Element
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: BSCHG
    description: >-
      Beam Section Property Changes ("CONSTANT" / "CHANGE")
    type: string
    requirement: optional
    documentedDefault: CHANGE
    documentedOptional: true
    enum: [CONSTANT, CHANGE]
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: CABINIT
    description: >-
      Consider Initial Tension for Cable Element
    type: boolean
    requirement: optional
    documentedDefault: true
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
  - key: TCELEM
    description: >-
      Tension/Compression Truss Element
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's MainControlDataHyperS case completed a live
      create-read-update-delete round trip on civil without this field in its
      payload. That is evidence the call is accepted, not that the resulting
      model is what an engineer wanted.
    provenance: manual
    properties:
      - key: NUMINC
        description: >-
          Number of Increments
        type: integer
        requirement: optional
        documentedDefault: 1
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: INTOUT
        description: >-
          Intermediate Output Request ("EVERY" / "LAST")
        type: string
        requirement: optional
        documentedDefault: LAST
        documentedOptional: true
        enum: [EVERY, LAST]
        safeToOmit: unverified
        provenance: manual
      - key: CONVERGENCE
        description: >-
          Convergence Criteria
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: OPT_USE
            description: >-
              해당 기준 사용 여부
            type: boolean
            requirement: unstated
            documentedDefault: null
            documentedOptional: null
            safeToOmit: unverified
            provenance: manual
          - key: VALUE
            description: >-
              허용오차 (OPT_USE = true일 때 필수)
            type: number
            requirement: unstated
            documentedDefault: null
            documentedOptional: null
            safeToOmit: unverified
            provenance: manual

sdkRules:
  - id: db-actl-m1-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-actl-m1-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 12_DB_Analysis_Control.md line 143
  table: Parameters — 기본 설정
  missingColumns:
    - heading: Parameters — TCELEM 객체 (인장/압축 트러스)
      line: 167
      columns: [Default]
  structuralTables:
    - heading: Parameters — TCELEM 객체 (인장/압축 트러스)
      line: 159
      paths:
        - TCELEM
    - heading: Parameters — TCELEM 객체 (인장/압축 트러스)
      line: 167
      paths:
        - TCELEM.CONVERGENCE
