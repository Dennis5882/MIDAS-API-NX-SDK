# /db/STAG
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-stag
endpoint: /db/STAG
name: Define Construction Stage

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 10_DB_Construction_Stage.md
    section: 1. /db/STAG – Define Construction Stage
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-07-29
    - product: gen
      ref: db-write-sweep-2026-07-29

operations:
  - method: DELETE
    variant: per_id
    path: /db/STAG/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      시공단계명
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DURATION
    description: >-
      시공단계 지속 기간 (일)
    type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: bSV_RSLT
    description: >-
      결과 저장 – 단계
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bSV_STEP
    description: >-
      결과 저장 – 추가 스텝
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bLOAD_STEP
    description: >-
      재료 비선형 해석을 위한 하중 증분 스텝 사용 여부
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: INCRE_STEP
    description: >-
      하중 증분 스텝 수 (bLOAD_STEP=true 시)
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's ConstructionStage case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: ADD_STEP
    description: >-
      추가 스텝 목록
    type: array
    items:
      type: number
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ACT_ELEM
    description: >-
      구조 그룹 활성화 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: GRUP_NAME
        description: >-
          활성화할 구조 그룹명
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ACT_ELEM' by numbering it '(1)', not
        # by naming a path
      - key: AGE
        description: >-
          재료 나이 (일)
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ACT_ELEM' by numbering it '(2)', not
        # by naming a path
  - key: DACT_ELEM
    description: >-
      구조 그룹 비활성화 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's ConstructionStage case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
    properties:
      - key: REDIST
        description: >-
          요소력 재분배 (%)
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'DACT_ELEM' by numbering it '(2)', not
        # by naming a path
  - key: ACT_BNGR
    description: >-
      경계 그룹 활성화 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: BNGR_NAME
        description: >-
          활성화할 경계 그룹명
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ACT_BNGR' by numbering it '(1)', not
        # by naming a path
      - key: POS
        description: >-
          지점/스프링 위치 ("DEFORMED" / "ORIGINAL")
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ACT_BNGR' by numbering it '(2)', not
        # by naming a path
  - key: DACT_BNGR
    description: >-
      비활성화할 경계 그룹명 목록
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's ConstructionStage case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: ACT_LOAD
    description: >-
      하중 그룹 활성화 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: LOAD_NAME
        description: >-
          활성화할 하중 그룹명
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ACT_LOAD' by numbering it '(1)', not
        # by naming a path
      - key: DAY
        description: >-
          활성 일 ("FIRST" / "LAST" / 숫자 문자열)
        type: string
        requirement: optional
        documentedDefault: FIRST
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'ACT_LOAD' by numbering it '(2)', not
        # by naming a path
  - key: DACT_LOAD
    description: >-
      하중 그룹 비활성화 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's ConstructionStage case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual

sdkRules:
  - id: db-stag-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-stag-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 10_DB_Construction_Stage.md line 58
  table: 1-2. 파라미터
