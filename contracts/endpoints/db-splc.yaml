# /db/SPLC
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-splc
endpoint: /db/SPLC
name: Response Spectrum Load Cases

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 09_DB_Dynamic_Loads.md
    section: 2. /db/SPLC – Response Spectrum Load Cases
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_civil
  records:
    - product: civil
      ref: db-write-sweep-2026-09-01

operations:
  - method: DELETE
    variant: per_id
    path: /db/SPLC/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      하중 케이스명
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DESC
    description: >-
      설명
    type: string
    requirement: optional
    documentedDefault: ""
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: DIR
    description: >-
      방향 ("XY" 또는 "Z")
    type: string
    requirement: optional
    documentedDefault: XY
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ANGLE
    description: >-
      가진 각도
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SCALE
    description: >-
      스케일 계수
    type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: PMFT
    description: >-
      주기 수정 계수
    type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: aFUNCNAME
    description: >-
      스펙트럼 함수명 목록
    type: array
    items:
      type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: INTERP
    description: >-
      스펙트럼 데이터 보간 방법 ("LINEAR" / "LOG")
    type: string
    requirement: optional
    documentedDefault: LINEAR
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: COMTYPE
    description: >-
      모드 조합 방법 ("SRSS" / "CQC" / "ABS" / "Linear")
    type: string
    requirement: optional
    documentedDefault: CQC
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bADDSIGN
    description: >-
      결과에 부호 추가
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: iSIGNTYPE
    description: >-
      부호 추가 방법 (0=주모드방향, 1=절대최대값방향)
    type: integer
    requirement: optional
    documentedDefault: 1
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bMODE
    description: >-
      모드 형상 선택
    type: boolean
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: aUSEMODE
    description: >-
      사용 모드 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: bUSE
        description: >-
          모드 사용 여부
        type: boolean
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'aUSEMODE' by numbering it '(1)', not
        # by naming a path
      - key: MSFACTOR
        description: >-
          모드 형상 계수
        type: number
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'aUSEMODE' by numbering it '(2)', not
        # by naming a path
  - key: bDAMP
    description: >-
      감쇠 방법 적용 여부
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bCDAMP
    description: >-
      감쇠비 보정 여부
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: iMDTYPE
    description: >-
      감쇠 방법 (1=Modal, 2=Mass&Stiff, 3=StrainEnergy)
    type: integer
    requirement: conditional
    condition: bDAMP=true 시
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

sdkRules:
  - id: db-splc-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-splc-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 09_DB_Dynamic_Loads.md line 625
  table: 2-2. 파라미터
  missingColumns:
    - heading: Modal 감쇠 추가 파라미터
      line: 648
      columns: [Default]
    - heading: "Mass & Stiffness Proportional 감쇠 추가 파라미터"
      line: 657
      columns: [Default]
  # Additional parameter tables in this section were NOT merged. They are
  # usually conditional variants selected by a type/code field. Decide
  # whether they belong in this contract's fields, as nested `properties`,
  # or as a separate contract - do not assume the first table is the whole
  # schema.
  unmergedTables:
    - heading: Modal 감쇠 추가 파라미터
      resolution: "The damping tables name modes but do not state a complete wire condition selecting each supplementary field set; left unmerged."
      fields: 2
      line: 648
    - heading: "Mass & Stiffness Proportional 감쇠 추가 파라미터"
      resolution: "The damping tables name modes but do not state a complete wire condition selecting each supplementary field set; left unmerged."
      fields: 10
      line: 657
    - heading: "Mass & Stiffness Proportional 감쇠 추가 파라미터"
      resolution: "The damping tables name modes but do not state a complete wire condition selecting each supplementary field set; left unmerged."
      fields: 6
      line: 672
    - heading: "Mass & Stiffness Proportional 감쇠 추가 파라미터"
      resolution: "The damping tables name modes but do not state a complete wire condition selecting each supplementary field set; left unmerged."
      fields: 1
      line: 692
