# /db/MVLD
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-mvld
endpoint: /db/MVLD
name: Moving Load Cases

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 08_DB_Moving_Loads.md
    section: 12. /db/MVLD – Moving Load Cases
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-17
    - product: gen
      ref: db-write-sweep-2026-08-17

operations:
  - method: DELETE
    variant: per_id
    path: /db/MVLD/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: LCNAME
    description: >-
      Load Case Name
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DESC
    description: >-
      Description
    type: string
    requirement: optional
    documentedDefault: ""
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: TYPE
    description: >-
      Load Type (0=General, 1=Permit, 2=Optimization)
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

variants:
  - when:
      - path: LOAD_MODEL
        equals: 2
    source:
      table: ASL (Australia Heavy Load Platform 병용, LOAD_MODEL=2)
      line: 1464
    fields:
      - key: MULTIPLE_FACTOR
        description: >-
          Unobstructed Lane Scale Factor(예: 0.5)
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: VEHICLE_LOAD_NAME
        description: >-
          Load Case Data — Heavy Load 차량 이름
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: VEHICLE_LOAD_NAME2
        description: >-
          Load Case Data — M1600/S1600 차량 이름
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: MIN_LOADED_LANE
        description: >-
          Min. Number of Loaded Lanes
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: MAX_LOADED_LANE
        description: >-
          Max. Number of Loaded Lanes
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: LINE_ITEMS
        description: >-
          Defined Lane
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: NA_LLAN_NAMES
            description: >-
              Selected Lanes
            type: array
            items:
              type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: STRAD_LLAN1_NAMES
            description: >-
              Heavy Load Lanes Start
            type: array
            items:
              type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: STRAD_LLAN2_NAMES
            description: >-
              Heavy Load Lanes End
            type: array
            items:
              type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
  - when:
      - path: TYPE
        equals: 1
    source:
      table: Parameters – PERMIT_LOAD (TYPE=1)
      line: 1489
    fields:
      - key: VEHICLE_LOAD_NAME
        description: >-
          Vehicle Load Name
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: REF_LANE
        description: >-
          Reference Lane
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: SCALE_FACTOR
        description: >-
          Scale Factor
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
  - when:
      - path: TYPE
        equals: 2
    source:
      table: Parameters – AUTO_OPTIMIZE (TYPE=2)
      line: 1500
    fields:
      - key: SCALE_FACTORS
        description: >-
          Multiple Presence Factor
        type: array
        items:
          type: number
        minItems: 6
        maxItems: 6
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: MIN_VEHL_DIST
        description: >-
          Min. Vehicle Distance
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: LANE_NAME
        description: >-
          Loaded Lane Name
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: MIN_NUM_VEHICLE
        description: >-
          Min. Number of Vehicle
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: MAX_NUM_VEHICLE
        description: >-
          Max. Number of Vehicle
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: LOAD_MODEL
        description: >-
          Select Load Model (Australia 전용, 값은 DEFAULT의 LOAD_MODEL과 동일)
        type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: FATIGUE
        description: >-
          Fatigue Option (Australia 전용)
        type: boolean
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: OPTIMIZE_ITEMS
        description: >-
          Optimize Items · VEHICLE_TYPE("VL"/"VC")/VEHICLE_NAME/SCALE_FACTOR
        type: array
        items:
          type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual

sdkRules:
  - id: db-mvld-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-mvld-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 08_DB_Moving_Loads.md line 1415
  table: Parameters – 공통
  missingColumns:
    - heading: Parameters – DEFAULT (Korea – 추가 필드)
      line: 1438
      columns: [Default]
    - heading: Parameters – DEFAULT (Australia – 추가 필드)
      line: 1456
      columns: [Default]
    - heading: ASL (Australia Heavy Load Platform 병용, LOAD_MODEL=2)
      line: 1464
      columns: [Default]
    - heading: Parameters – DEFAULT (Russia – 추가 필드)
      line: 1481
      columns: [Default]
    - heading: Parameters – PERMIT_LOAD (TYPE=1)
      line: 1489
      columns: [Default]
    - heading: Parameters – AUTO_OPTIMIZE (TYPE=2)
      line: 1500
      columns: [Default]
  # Additional parameter tables in this section were NOT merged. They are
  # usually conditional variants selected by a type/code field. Decide
  # whether they belong in this contract's fields, as nested `properties`,
  # or as a separate contract - do not assume the first table is the whole
  # schema.
  unmergedTables:
    - heading: Parameters – DEFAULT (General Load, KSCE-LSD15 / AASHTO STD / LRFD / PENNDOT / Taiwan / Canada)
      resolution: "the manual labels these national/default field groups but gives no wire selector value; left unmerged"
      fields: 4
      line: 1423
    - heading: Parameters – DEFAULT (Korea – 추가 필드)
      resolution: "the manual labels these national/default field groups but gives no wire selector value; left unmerged"
      fields: 5
      line: 1438
    - heading: Parameters – DEFAULT (Australia – 추가 필드)
      resolution: "the manual labels these national/default field groups but gives no wire selector value; left unmerged"
      fields: 3
      line: 1456
    - heading: Parameters – DEFAULT (Russia – 추가 필드)
      resolution: "the manual labels these national/default field groups but gives no wire selector value; left unmerged"
      fields: 1
      line: 1481
