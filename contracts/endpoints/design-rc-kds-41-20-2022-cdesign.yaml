# /DESIGN/RC/KDS-41-20-2022/CDESIGN
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-cdesign
endpoint: /DESIGN/RC/KDS-41-20-2022/CDESIGN
name: RC Concrete Design Result

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "66. `DESIGN/RC/KDS-41-20-2022/CDESIGN` — RC Concrete Design Result (RC 콘크리트 종합 설계 결과)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: EXPORT_PATH
    description: >-
      이미지 저장 경로+파일명
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: FIGURE_NAME
    description: >-
      스마트 리포트 이미지 이름
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: WIDTH
    description: >-
      이미지 가로 픽셀 (100–10000)
    type: integer
    minimum: 100
    maximum: 10000
    requirement: optional
    documentedDefault: 1000
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: HEIGHT
    description: >-
      이미지 세로 픽셀 (100–10000)
    type: integer
    minimum: 100
    maximum: 10000
    requirement: optional
    documentedDefault: 1000
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: STAGE_NAME
    description: >-
      시공단계 이름
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SET_HIDDEN
    description: >-
      Hidden 표시 옵션
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ACTIVE
    description: >-
      화면 Active/Angle/Display 설정
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ANGLE
    description: >-
      화면 Active/Angle/Display 설정
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: DISPLAY
    description: >-
      화면 Active/Angle/Display 설정
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: PERSPECTIVE
    description: >-
      투시(원근) 뷰
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ZOOM_LEVEL
    description: >-
      줌 레벨 (25=축소, 100=fit, 200=최대)
    type: number
    minimum: 25
    maximum: 200
    requirement: optional
    documentedDefault: 100
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: BGCOLOR_TOP
    description: >-
      배경색 상/하단 (RGB)
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: BGCOLOR_BOTTOM
    description: >-
      배경색 상/하단 (RGB)
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: RESULT_GRAPHIC
    description: >-
      설계결과 그래픽 설정
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: LOAD_CASE_COMB
        description: >-
          하중케이스/조합 (TYPE="CBC", NAME)
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.1', not by naming a path
      - key: COMPONENTS
        description: >-
          강도비 성분 (Axial/Shear-y/Shear-z/Bend-y/Bend-z/Combined)
        type: string
        requirement: optional
        documentedDefault: Combined
        documentedOptional: true
        enum: [Axial, Shear-y, Shear-z, Bend-y, Bend-z, Combined]
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.2', not by naming a path
      - key: TYPE_OF_DISPLAY
        description: >-
          표시 옵션 (CONTOUR/LEGEND/VALUES)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.3', not by naming a path
      - key: REINFORCEMENT
        description: >-
          배근 표시 여부
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.4', not by naming a path
      - key: REINFORCEMENT_TYPE
        description: >-
          배근 표시 타입 (REBAR/AREA/RATIO)
        type: string
        requirement: optional
        documentedDefault: REBAR
        documentedOptional: true
        enum: [REBAR, AREA, RATIO]
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.5', not by naming a path
      - key: DISPLAY_MEMBERS
        description: >-
          표시 부재 종류 (BEAM/COLUMN/BRACE/WALL)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.6', not by naming a path
      - key: OUTPUT_COMPONENT
        description: >-
          출력 성분 (RATIO_AXIAL_STRESS/MAIN_REBAR/SHEAR_REINFORCEMENT)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.7', not by naming a path
      - key: COLUMN_SECTION_SIZE
        description: >-
          기둥 단면 크기 표시 (SCALE_FACTOR 0.1–100)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.8', not by naming a path
      - key: VALUE_OPTION
        description: >-
          값 표시 형식 (DECIMAL_PLACES/EXPONENTIAL)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.9', not by naming a path
      - key: OUTPUT_SECT_LOCATION
        description: >-
          출력 단면 위치 (OPT_I/OPT_CENTER_MID/OPT_J/OPT_MAX/OPT_ALL)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # NOTE: the manual nests this under 'RESULT_GRAPHIC' by numbering it
        # '12.10', not by naming a path

extraction:
  source: 26_Design_RC_KDS41202022.md line 12758
  table: 파라미터
