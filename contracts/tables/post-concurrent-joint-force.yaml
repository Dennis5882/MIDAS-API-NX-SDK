# Source: 19_POST_AnalysisResult_1.md section 13. No SDK source was used.
contractVersion: 1
id: post-concurrent-joint-force
endpoint: /post/TABLE
name: Concurrent Joint Force
summary: Concurrent forces at a selected reaction node.
products: [gen, civil]
source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 19_POST_AnalysisResult_1.md
    section: "13. Concurrent Joint Force"
verification: {status: manual_only}
risk: read_only
tableTypes:
  - value: CONCURRENT_JOINT_FORCE
    description: Concurrent joint force
    headNote: >-
      HEAD begins Index, Elem., Load, then repeats one 13-column block per
      selected reaction component; the number of blocks is selected by the
      required ADDITIONAL.SET_REACTION_PARAMS.COMPONENT value.
    provenance: manual
requestFields:
  unsupported: [NODE_ELEMS, OPT_CS, STAGE_STEP]
  required: [LOAD_CASE_NAMES]
  additional:
    - key: ADDITIONAL
      description: Contains the required SET_REACTION_PARAMS object for the selected reaction node and components.
      type: object
      requirement: required
      provenance: manual
